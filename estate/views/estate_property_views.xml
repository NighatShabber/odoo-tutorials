<odoo>

    <!-- ================= ACTIONS ================= -->

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available': 1}</field>
    </record>

    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">list,form</field>
    </record>


    <!-- ================= PROPERTY LIST VIEW ================= -->

    <record id="estate_property_list_view" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-info="state == 'offer_received'"
                  decoration-bf="state == 'offer_accepted'"
                  decoration-success="state == 'sold'">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="postcode"/>
                <field name="date_availability" optional="hide"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
            </list>
        </field>
    </record>


    <!-- ================= PROPERTY FORM VIEW ================= (UPDATED) -->

    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('sold','canceled')"/>
                    <button name="action_sold" type="object" string="Sold"
                            class="btn-primary" invisible="state != 'offer_accepted'"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <h1><field name="name"/></h1>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True, 'no_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price" readonly="1"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                    <notebook>
                        <!-- Description tab: includes description, bedrooms, facades, and area fields -->
                        <page string="Description">
                            <group>
                                <field name="description" placeholder="Write a description..."/>
                            </group>
                            <group>
                                <group>
                                    <field name="bedrooms"/>
                                    <field name="facades"/>
                                    <field name="garden"/>
                                </group>
                                <group>
                                    <field name="living_area" string="Living Area (sqm)"/>
                                    <field name="garden_area" string="Garden Area (sqm)" invisible="not garden"/>
                                    <field name="garden_orientation" invisible="not garden"/>
                                    <field name="total_area" string="Total Area (sqm)" readonly="1"/>
                                </group>
                            </group>
                        </page>
                        <!-- Offers tab -->
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ('offer_accepted','sold','canceled')">
                                <list editable="bottom"
                                      decoration-danger="state == 'refused'"
                                      decoration-success="state == 'accepted'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <button name="action_accept" type="object" string="Accept"
                                            class="btn-primary" invisible="state in ('accepted','refused')"/>
                                    <button name="action_refuse" type="object" string="Refuse"
                                            class="btn-secondary" invisible="state in ('accepted','refused')"/>
                                </list>
                            </field>
                        </page>
                        <!-- Other Info tab -->
                        <page string="Other Info">
                            <field name="buyer_id"/>
                            <field name="salesperson_id"/>
                            <field name="state"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>


    <!-- ================= PROPERTY SEARCH VIEW ================= -->

    <record id="estate_property_search_view" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="postcode"/>
                <field name="property_type_id"/>
                <field name="tag_ids"/>
                <filter name="available" string="Available" domain="[('state', '=', 'new')]"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
            </search>
        </field>
    </record>


    <!-- ================= OFFER LIST VIEW ================= -->

    <record id="estate_property_offer_list_view" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="bottom"
                  decoration-danger="state == 'refused'"
                  decoration-success="state == 'accepted'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="property_id"/>
                <field name="property_type_id" optional="hide"/>
            </list>
        </field>
    </record>


    <!-- ================= OFFER FORM VIEW ================= -->

    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_accept" string="Accept" type="object" class="btn-primary"
                            invisible="state != False"/>
                    <button name="action_refuse" string="Refuse" type="object" invisible="state != False"/>
                </header>
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="property_id"/>
                        <field name="state"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>


    <!-- ================= PROPERTY TYPE LIST VIEW ================= -->

    <record id="estate_property_type_list_view" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </list>
        </field>
    </record>


    <!-- ================= PROPERTY TYPE FORM VIEW ================= -->

    <record id="estate_property_type_form_view" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(estate_property_offer_action)d" type="action"
                                class="oe_stat_button" icon="fa-handshake-o">
                            <field name="offer_count" widget="statinfo"/>
                        </button>
                    </div>
                    <group>
                        <field name="name"/>
                    </group>
                    <group string="Properties">
                        <field name="property_ids">
                            <list>
                                <field name="name"/>
                                <field name="expected_price"/>
                                <field name="state"/>
                            </list>
                        </field>
                    </group>
                </sheet>
            </form>
        </field>
    </record>


    <!-- ================= TAG LIST VIEW ================= -->

    <record id="estate_property_tag_list_view" model="ir.ui.view">
        <field name="name">estate.property.tag.list</field>
        <field name="model">estate.property.tag</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
                <field name="color"/>
            </list>
        </field>
    </record>


    <!-- ================= INHERITED USER FORM VIEW ================= -->

    <record id="view_users_form_inherit_estate" model="ir.ui.view">
        <field name="name">res.users.form.inherit.estate</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Real Estate Properties">
                    <field name="property_ids">
                        <list>
                            <field name="name"/>
                            <field name="expected_price"/>
                            <field name="state"/>
                            <field name="date_availability"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>


    <!-- ================= MENUS ================= -->

    <menuitem id="estate_menu_root" name="Real Estate"/>
    <menuitem id="estate_menu_advertisements" name="Advertisements" parent="estate_menu_root"/>
    <menuitem id="estate_property_menu" name="Properties" parent="estate_menu_advertisements" action="estate_property_action"/>
    <menuitem id="estate_menu_settings" name="Settings" parent="estate_menu_root"/>
    <menuitem id="estate_property_type_menu" name="Property Types" parent="estate_menu_settings" action="estate_property_type_action"/>
    <menuitem id="estate_property_tag_menu" name="Tags" parent="estate_menu_settings" action="estate_property_tag_action"/>

</odoo>